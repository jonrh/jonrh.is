{
  "name": "jonrh.is",
  "description": "Personal website, statically generated with GatbsyJS",
  "author": "Jón Rúnar Helgason",

  "version": "1.0.0",
  "license": "MIT",
  "repository": "https://github.com/Rusta/gatsby-starter-barebones",

  "dependencies": {
    "gatsby": "^0.12.45",
    "react": "^15.4.2",
    "react-helmet": "^5.0.3"
  },

  "main": "n/a",

  "scripts": {
    "build": "gatsby build",
    "deploy": "gatsby build --prefix-links && gh-pages -d public",
    "develop": "gatsby develop",
    "format": "prettier --print-width 100 --trailing-comma all --write \"pages/*.js\" \"utils/*.js\" \"wrappers/*.js\" \"html.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
